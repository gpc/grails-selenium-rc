- Default selenium.url from grails serverURL
- Configuration for
   - screenshot dir
   - screenshot capture (always, onfailure, off)
- Config override via system properties
- Nice delegation of calls from GrailsSeleneseTestCase to Selenium and SeleniumTestBase instances
- Dynamic(?) assert* verify* & waitFor* methods
- ensure not loaded in war / non test mode
- handle context path automatically